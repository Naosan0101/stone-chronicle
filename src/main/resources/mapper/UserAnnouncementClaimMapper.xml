<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nineuniverse.repository.UserAnnouncementClaimMapper">

	<insert id="insertIfAbsent">
		INSERT INTO user_announcement_claim (user_id, announcement_key)
		VALUES (#{userId}, #{announcementKey})
		ON CONFLICT (user_id, announcement_key) DO NOTHING
	</insert>

	<select id="exists" resultType="boolean">
		SELECT EXISTS(
			SELECT 1 FROM user_announcement_claim
			WHERE user_id = #{userId} AND announcement_key = #{announcementKey}
		)
	</select>

	<select id="findClaimedKeys" resultType="string">
		SELECT announcement_key
		FROM user_announcement_claim
		WHERE user_id = #{userId}
	</select>
</mapper>
